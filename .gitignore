*.backup
.DS_Store
password
token
coverage.out
.mermaid-tmp/

# Test cache (for git hook optimization)
.test-cache/
gravwell.token

# Build artifacts (copied from root at build time)
config-test/.yamllint

# Devcontainer secrets (written by initializeCommand, consumed by postCreateCommand)
.devcontainer/.gh-token

# Claude Code artifacts
claude-review-output.jsonl
